#include "<df>/platform/aisix/aisix.h"

extern Main

extern HeapInit

procedure _DF_DF0 { heapbase heapsize -- }
	if (heapsize@ 1024 >)
		heapbase@ heapsize@ 1024 - HeapInit
	end else
		-1 Exit
	end

	Main

	0 Exit
end